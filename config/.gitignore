# Local configuration overrides (not version controlled)
config.local.yaml

# Secrets file - contains sensitive credentials
secrets.yaml
services.secrets.yaml
*.secrets.yaml

# Backup files
*.bak
*.backup
*.tmp
